import { Check, ChevronRight } from 'lucide-react';
import { FramePrediction, CONDITION_COLORS } from '@/types/analysis';
import { formatTimestamp } from '@/lib/utils';

export function ConditionTimeline({ frames }: { frames: FramePrediction[] }) {
  return <div className="glass-card p-5 md:p-6"><div className="mb-6 flex items-center justify-between"><div><div className="text-xs font-semibold uppercase tracking-[0.2em] text-muted-foreground">Condition timeline</div><div className="mt-1 text-sm text-muted-foreground">Visual observations by frame</div></div><div className="rounded-full border border-white/10 px-3 py-1 text-[10px] uppercase tracking-wider text-muted-foreground">{frames.length} frames</div></div><div className="flex items-start overflow-x-auto pb-2">{frames.map((frame, index) => <div key={`${frame.timestamp}-${index}`} className="flex shrink-0 items-start"><div className="w-24 text-center"><div className="mb-3 text-[10px] font-mono text-muted-foreground">{formatTimestamp(frame.timestamp)}</div><div className="mx-auto flex h-10 w-10 items-center justify-center rounded-full border-4 border-background" style={{ backgroundColor: CONDITION_COLORS[frame.condition], boxShadow: `0 0 16px ${CONDITION_COLORS[frame.condition]}55` }}><Check className="h-4 w-4 text-black/70" /></div><div className="mt-3 text-[10px] font-bold tracking-wide" style={{ color: CONDITION_COLORS[frame.condition] }}>{frame.condition}</div><div className="mt-1 text-[10px] text-muted-foreground">{Math.round(frame.confidence * 100)}%</div></div>{index < frames.length - 1 && <ChevronRight className="mt-8 h-4 w-4 shrink-0 text-white/20" />}</div>)}</div></div>;
}
