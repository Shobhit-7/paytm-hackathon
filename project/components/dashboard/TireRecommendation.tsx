import { ArrowRight, CircleAlert, Gauge, Radio } from 'lucide-react';
import { Recommendation } from '@/types/analysis';
import { formatConfidence, getTrendColor } from '@/lib/utils';

export function TireRecommendation({ recommendation }: { recommendation: Recommendation }) {
  return <div className="glass-card overflow-hidden"><div className="border-b border-white/5 bg-white/[0.02] p-5 md:p-6"><div className="flex items-center justify-between"><div><div className="text-xs font-semibold uppercase tracking-[0.2em] text-muted-foreground">Tyre window</div><div className="mt-2 flex items-center gap-2 text-xl font-bold"><span className="text-racing-yellow">{recommendation.tyre_window?.split(' → ')[0]}</span>{recommendation.tyre_window?.includes('→') && <ArrowRight className="h-4 w-4 text-muted-foreground" />}<span className="text-racing-green">{recommendation.tyre_window?.split(' → ')[1]}</span></div></div><div className="rounded-lg border border-racing-yellow/30 bg-racing-yellow/10 px-3 py-2 text-center"><div className="text-[10px] uppercase tracking-wider text-racing-yellow">Status</div><div className="mt-1 text-xs font-bold text-racing-yellow">{recommendation.window_status}</div></div></div></div><div className="space-y-5 p-5 md:p-6"><div className="flex gap-3"><CircleAlert className="mt-0.5 h-4 w-4 shrink-0 text-racing-yellow" /><div><div className="text-sm font-semibold">{recommendation.suggestion}</div><p className="mt-1 text-xs leading-5 text-muted-foreground">{recommendation.reason}</p></div></div><div className="grid grid-cols-2 gap-3 border-t border-white/5 pt-4"><div className="flex items-center gap-2 text-xs"><Gauge className="h-4 w-4 text-muted-foreground" /><span className="text-muted-foreground">Confidence</span><span className="ml-auto font-semibold">{formatConfidence(recommendation.confidence)}</span></div><div className="flex items-center gap-2 text-xs"><Radio className="h-4 w-4 text-racing-green" /><span className="text-muted-foreground">Trend</span><span className={`ml-auto font-semibold ${getTrendColor(recommendation.trend)}`}>{recommendation.trend}</span></div></div></div></div>;
}
